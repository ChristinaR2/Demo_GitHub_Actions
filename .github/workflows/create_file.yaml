name: Create file using self-hosted runner

on:
  workflow_dispatch: 

jobs:
  create-file:
    runs-on: self-hosted

    steps:
      - name: create file
        run: |
          "File created successfully" > C:\Temp\github-test.txt
        shell: powershell
